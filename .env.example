# General
APP_ENV=production
WORKDIR=/app

# OpenAI (used in hook_generator). Provide your key (if using OpenAI)
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY

# Buffer API (or other posting API)
BUFFER_ACCESS_TOKEN=REPLACE_WITH_BUFFER_TOKEN

# Optional Unsplash (if you want programmatic image search)
UNSPLASH_ACCESS_KEY=

# Scheduling
SCHEDULE_CRON="0 * * * *"   # every hour (cron style) -- used if scheduler cron mode enabled

# Paths (optional)
BACKGROUND_DIR=data/backgrounds
HOOKS_DIR=data/hooks
COMPOSED_DIR=data/composed
PRODUCTS_DIR=data/products

# Misc
DEFAULT_POSTS_PER_DAY=10
